<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="robots" content="noindex, nofollow, noarchive">
    <title>
        <%= title || 'ChurchKite Admin' %>
    </title>
    <link rel="stylesheet" href="/css/styles.css">
</head>

<body>
    <header class="header">
        <div class="logo">ChurchKite</div>
        <nav class="nav">
            <a href="/plugins" class="<%= page === 'plugins' ? 'active' : '' %>">Plugins</a>
            <a href="/sites" class="<%= page === 'sites' ? 'active' : '' %>">Sites</a>
            <a href="/inventory" class="<%= page === 'inventory' ? 'active' : '' %>">Inventory</a>
        </nav>
        <div class="user-actions">
            <% if (!isProd) { %>
                <div class="badge">Local Mode</div>
                <% } %>
                    <% if (isProd) { %>
                        <a href="/logout" class="btn btn-primary">Logout</a>
                        <% } %>
        </div>
    </header>

    <main class="main-content">
        <%- body %>
    </main>

</body>

</html>
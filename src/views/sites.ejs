<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="robots" content="noindex, nofollow, noarchive">
    <title>Registered Sites</title>
    <link rel="stylesheet" href="/css/styles.css">
    <script defer src="/js/app.js"></script>
    <style>
        .container {
            max-width: 960px;
            margin: 2rem auto;
        }

        .nav {
            margin-bottom: 1rem;
        }

        .nav a {
            margin-right: .75rem;
        }

        .card {
            border: 1px solid #ddd;
            border-radius: .5rem;
            margin-bottom: 1.5rem;
        }

        .card-header {
            background: #f5f5f5;
            padding: 1rem;
            border-bottom: 1px solid #ddd;
        }

        .card-body {
            padding: 1rem;
        }

        table {
            width: 100%;
            border-collapse: collapse;
        }

        th,
        td {
            border: 1px solid #ddd;
            padding: 8px;
        }

        th {
            background: #f5f5f5;
            text-align: left;
        }
    </style>
</head>

<body>
    <div class="container">
        <% if (!isProd) { %>
            <div class="badge badge-local" style="margin:.5rem 0;">Local Mode</div>
            <% } %>
                <div class="nav nav-colored">
                    <a href="/plugins">Plugins</a>
                    <a href="/sites">Sites</a>
                    <a href="/inventory">Inventory</a>
                    <span class="spacer"></span>
                    <% if (isProd) { %>
                        <a href="/logout">Logout</a>
                        <% } %>
                </div>

                <div class="page-header">
                    <h1>Registered Sites</h1>
                </div>

                <% if (!sites || sites.length===0) { %>
                    <div class="alert alert-warning">No sites registered yet. Activate the connector on a WordPress site
                        to see it here.</div>
                    <% } else { %>
                        <div class="card">
                            <div class="card-header">
                                <h2>All Sites</h2>
                            </div>
                            <div class="card-body">
                                <table class="table">
                                    <thead>
                                        <tr>
                                            <th>Site URL</th>
                                            <th>Last Seen</th>
                                            <th>Plugins</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        <% for (const site of (sites || [])) { %>
                                            <tr>
                                                <td><a href="/plugins?site=<%= encodeURIComponent(site.siteUrl) %>">
                                                        <%= site.siteUrl %>
                                                    </a></td>
                                                <td>
                                                    <%= site.lastSeen %>
                                                </td>
                                                <td>
                                                    <%= (site.plugins || []).join(', ') %></td>
                                            </tr>
                                        <% } %>
                                    </tbody>
                                </table>
                            </div>
                        </div>
                <% } %>
    </div>
</body>
</html>